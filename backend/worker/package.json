{
  "name": "api-radar-worker",
  "version": "1.0.0",
  "description": "API Radar Cloudflare Worker (Phase 1)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}