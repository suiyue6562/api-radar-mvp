# API优选咨询 / API Radar v2 —— 2026-09-21 晚间状态

## 当前状态（你下班时）

| 项 | 状态 |
|---|---|
| 代码 | 已推到 GitHub commit `b76d293`（P0 跳转修复 + cache-bust + 域名修） |
| 服务器 | ⚠️ **HTTP 502**——Python http.server 进程挂了 |
| Cloudflare Tunnel | 通（页面访问得到 502 = 隧道在转，只是源头死了）|
| Worker | OK（api.youxuan-api.suiyue6562.workers.dev / api.apireader.top / admin.apireader.top）|
| 数据 | 21/60/40/1287/13 完整 |

## 明天 SSH 跑这一段（30 秒搞定）

```bash
cd ~/api-radar && git pull origin main

pkill -f "http.server 8080" 2>/dev/null
pkill -f watchdog_python 2>/dev/null
sleep 2

cd ~/api-radar/03_prototype
nohup python3 -m http.server 8080 --bind 0.0.0.0 > /tmp/radar.log 2>&1 &

# 启动 watchdog（让以后再死自动重启）
nohup bash ~/api-radar/tests/watchdog_python.sh > /tmp/watchdog.log 2>&1 &

sleep 3
echo "=== 验证 ==="
curl -sk http://127.0.0.1:8080/ | grep "<title>"
pgrep -f "http.server 8080"
pgrep -f watchdog_python
```

## 本轮已完成修复（你看不到效果，因为服务器死了）

1. **app.js 重写 15KB**：含 NAV 数组（10 个入口）、PAGE_INITIALIZERS（每页初始化分发器）、全功能（导航/统计/模型网格/价格表/计算器/搜索/性价比榜/行情/Tab/流量上报）
2. **12 个页加 `?v=20260921b` cache-bust**：破坏 Cloudflare 旧 0 字节缓存
3. **admin.html 错误域名修复**：`api.apiyouxuan.top` → `api.apireader.top`
4. **每页加 PAGE_INITIALIZERS 调用**：让每页正确初始化，active 状态对
5. **watchdog 脚本就绪**：`tests/watchdog_python.sh`（每 30 秒健康检查+自动重启）

## 后台任务（你下班时还在跑）

3 个子代理：
- ✅ sa-2：读完 5 份文档（PRD/部署/admin/数据 schema）
- 🔄 sa-0：调研 10+ 优秀站点（OpenRouter/HF/Replicate/302.AI/LLMmap 等）
- 🔄 sa-1：检测 13 个页面跳转/渲染/缓存问题

完成后会自动汇总：
- 调研清单（15+ 条可借鉴优势）
- 页面 P0 问题清单
- 下阶段功能路线图（按优先级）

## 明天我继续做什么

- 根据调研 + 检测结果，写一份**3 天滚动开发计划**
- 落地 P0 修复（依赖子代理输出）
- 优化 Playground / 商家 / 后台
- 持续 git push（你 SSH 拉一次后即可）

## 关键经验（下次同类问题用）

- **必须 watchdog**（已写好，服务器上还需启动）
- **必须 cache-bust**（已加 `?v=20260921b`）
- **域名变更要全局 grep**（sed 替换时小心 `apireader.top` → `apiyouxuan.top` 会污染子域名）
- **Cloudflare 会缓存 0 字节文件**——一旦缓存很难破，必须加版本号