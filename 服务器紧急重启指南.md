# ⚠️ 紧急：服务器需要重启拉新代码

## 问题

服务器仍在跑**旧版本 index.html**（HTTP server 没刷新代码缓存）。

```
本地最新 commit d4d62a7（已推送 GitHub）
但 https://apireader.top/index.html 返回的仍是旧 inline script（无 setTimeout 重试）
```

## SSH 30 秒搞定

```bash
cd ~/api-radar && git pull origin main
pkill -f "http.server 8080" 2>/dev/null
sleep 2
cd ~/api-radar/03_prototype
nohup python3 -m http.server 8080 --bind 0.0.0.0 > /tmp/radar.log 2>&1 &
nohup bash ~/api-radar/tests/watchdog_http.sh > /tmp/watchdog.log 2>&1 &

sleep 3
echo "=== 验证 ==="
curl -sk "http://127.0.0.1:8080/index.html" | grep -c "setTimeout(updateHeroTagline"
# 应该输出 3
echo "=== 浏览器再次强刷 ==="
curl -sk "https://apireader.top/index.html" | grep -c "setTimeout(updateHeroTagline"
# 应该输出 3
```

如果第一条 curl 输出 0 —— git pull 没拉到新文件。请检查：
```bash
cd ~/api-radar
git log --oneline -3
ls -la 03_prototype/index.html
```

## 浏览器侧

服务器刷新后，浏览器**强刷**：
- **Ctrl+Shift+R**（硬刷新，忽略缓存）
- 或 **Ctrl+F5**
- 或**无痕模式**（Ctrl+Shift+N）打开 https://apireader.top/

## 现在数据应该显示

- hero-tagline: `实时数据 · 38 模型 · 32 渠道 · 193 个价格 · 13 项硬核检测`
- KPI 卡：基于真实 data.js 统计
- 7 天日历：基于真实 events 日期分布