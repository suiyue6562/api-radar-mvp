// API观察者 · 广告主排序权重表（人工唯一维护入口）
// =====================================================
// 用法：把已收录站点的 id 写进列表并设置 boost（0~15 的整数）。
//   - boost 会直接加到该站点的 AI 综合分上参与排序（上限 100）
//   - boost > 0 的站点在排行榜与详情页会显示「广告」标识（明示，不隐藏）
//   - 其余排序全部由 AI 依据实测数据自动完成，人工不干预
// 示例（已注释，取消注释即生效）：
//   {id:'p_openrouter', boost:8},
//
// 站点 id 可在 providers.html 详情页 URL 的 #id= 参数里看到。
// 此文件每日两次随数据管线自动刷新缓存版本号，直接改完 push 即生效。
var SPONSORED_RANK = [
];
