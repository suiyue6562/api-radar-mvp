# 服务器恢复指南

## 问题

服务器 http.server 进程死了 + Worker 端 API 也连不上。
所有访问 apireader.top 都返回 Cloudflare 502 Bad Gateway 错误页。

## 修复步骤

### 1. SSH 登录服务器

```bash
ssh admin@你的服务器
```

### 2. 检查现状

```bash
echo "=== 进程检查 ==="
ps aux | grep -E "http.server|cloudflared" | grep -v grep

echo ""
echo "=== 端口检查 ==="
ss -tlnp | grep -E "8080|80" 2>&1 || netstat -tlnp | grep -E "8080|80" 2>&1

echo ""
echo "=== 拉最新代码 ==="
cd ~/api-radar && git pull origin main
```

### 3. 重启 http.server

```bash
cd ~/api-radar/03_prototype

# 杀掉旧进程
pkill -f "http.server 8080" 2>/dev/null
sleep 2

# 启动新进程
nohup python3 -m http.server 8080 --bind 0.0.0.0 > /tmp/radar.log 2>&1 &

sleep 3
echo "=== 验证主站 ==="
curl -sk http://127.0.0.1:8080/ | grep "<title>"
ls -la ~/api-radar/03_prototype/app.js
```

### 4. 重启 Cloudflare Tunnel

```bash
# 检查 systemd 服务
systemctl status cloudflared-tunnel 2>&1 | head -5

# 如果没起来
sudo systemctl enable --now cloudflared-tunnel
```

### 5. 启动 watchdog（防再死）

```bash
# 看 watchdog 脚本是否在跑
pgrep -f watchdog_http
pgrep -f cloudflared_restart
pgrep -f auto_pull

# 没跑就启动
nohup bash ~/api-radar/tests/watchdog_http.sh > /tmp/watchdog.log 2>&1 &
nohup bash ~/api-radar/tests/cloudflared_restart.sh > /tmp/cloudflare_watchdog.log 2>&1 &
nohup bash ~/api-radar/tests/auto_pull.sh > /tmp/auto_pull.log 2>&1 &

sleep 5
pgrep -f watchdog
```

### 6. Worker API 检查

如果是 api.apireader.top 也 502，可能：
- Worker 代码有问题 → `cd ~/api-radar/backend/worker && npx wrangler deploy`
- Custom Domain 配置掉了 → Dashboard 重新绑

### 8. 验证

```bash
# 本地
curl -sk http://127.0.0.1:8080/ | grep "<title>"

# 通过隧道
curl -sk https://apireader.top/ | grep "<title>"

# Worker API
curl -sk https://api.apireader.top/api/data | head -c 200
```

## 跑完把输出贴给我

确认 502 消失。