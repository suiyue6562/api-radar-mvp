# API优选咨询 —— 3 天滚动开发计划

> 制定时间：2026-09-21 晚间
> 战略依据：10 个优秀站点调研 + 13 页现状审计
> 用户核心需求（已识别）：防假 API / 数据透明 / 一键上手 / 信息架构一致

---

## 🎯 战略一句话

**学 OpenRouter + models.dev + llm-stats：把"API 碎片化"作为切入点，用数据透明 + 场景化筛选 + 硬核真假检测 + agent 友好，把切换/检测/比较的成本压到 0。**

---

## Day 1 (今晚 — 9-22 凌晨)

### 已完成 ✅
- [x] 13 页跳转逻辑全面修复（统一 NAV + 按页初始化 + cache-bust）
- [x] admin.html 错误域名修复
- [x] app.js 重写（15KB → 16KB，10 入口导航 + 按页分发）
- [x] watchdog 脚本就绪（防 http.server 再死）
- [x] 调研报告完成（10 站点 17KB）

### 进行中 🔄
- [ ] data.js 扩展（10+ 维价格 + 趋势 + best_for + 综合分数）
- [ ] index.html 重做（趋势榜 + Best for X + 能力图标 + agent 友好）
- [ ] models.html 重做（卡片加趋势/分数/best_for/能力图标）
- [ ] llms.txt / openapi.json / mcp.json 静态文件
- [ ] renderModelGrid 升级版（显示周趋势 + 综合分）

### 完成指标
- [ ] GitHub commit 数量 ≥ 5
- [ ] 子代理 3 个完成率 100%
- [ ] 写完用户醒来就生效的代码

---

## Day 2 (9-22)

### 用户 SSH 重启服务器后立刻验证
- [ ] apireader.top 主页 HTTP 200
- [ ] 4 个 KPI 卡显示真实数据（60/40/1287/13）
- [ ] 新加的 Featured Models 趋势榜渲染
- [ ] 新加的 Best for X 场景榜首渲染
- [ ] models.html 卡片含 🧠 / 📈 / Best for 标签
- [ ] llms.txt / openapi.json / mcp.json 可访问

### 当天新增功能
1. **Playground 升级** — 加 export PDF/JSON 检测报告 + 加"批量测"（一个 base_url + 5 个 model 同时跑）+ 加"历史记录"（localStorage 存最近 10 次测试）
2. **business.html 商家页升级** — 加"接入状态指示灯"（🟢 在线/🔴 离线/🟡 部分）+ 加"用户评分"（5 星可选）+ 加"联系按钮"（mailto + 跳转到商家页）
3. **admin.html 升级** — 加"批量审核"按钮（一次勾选 10 条）+ 加"导出选中" + 加"埋点统计"（每条数据被查看次数）

---

## Day 3 (9-23)

### Day 2 完成后继续
1. **价格预测 P3 调研**（已 PRD 标注 v3 再考虑，**这次不主动调研**，等用户决定）
2. **Telegram bot @api_youxuan_bot** — 简单 echo 机器人，接 Cloudflare Worker
3. **邮件订阅 Resend 接入**（PRD P1，W3 计划）
4. **SEO/GEO** — sitemap.xml + robots.txt + Open Graph meta

### 用户体验优化
1. 移动端适配（响应式 grid 改为 flex，touch target ≥ 44px）
2. 暗/亮主题切换（user setting + localStorage）
3. 加载状态优化（skeleton screen）
4. 404 友好页（CF 自定义 error）

---

## 关键决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 价格字段维度 | 10+ 维（input/output/reasoning/cache_read/cache_write/audio） | models.dev 范式，把隐藏成本摊到阳光下 |
| 趋势数据 | 估算（基于 popularity） | 没有真实用户数据，先用合理估算 |
| Best for 场景 | coding / reasoning / throughput / cheapest / long_context / multimodal | llm-stats 范式，6 维度覆盖主要选型场景 |
| 综合分数算法 | 30% input + 40% output + 30% context | 输出价对成本影响最大 |
| Agent 友好 | OpenAPI + MCP + llms.txt 三暴露 | OpenRouter 2026 范式 |
| 调研瘫痪红线 | 不主动加 P3/P2 调研 | 用户明确反对调研瘫痪 |
| 不做 | 价格预测、邮件订阅（Day3 才做）、价格预测详情 | 聚焦核心 |

---

## 关键风险

| 风险 | 缓解 |
|---|---|
| 服务器 http.server 死了（当前 502） | watchdog 脚本明天 SSH 启 + cron 永久保活 |
| Cloudflare 缓存 app.js 0 字节 | 已加 `?v=20260921b/c/d` cache-bust |
| 用户访问旧版 | 浏览器硬刷即可；CF 旧缓存自动过期（< 1 小时） |
| 调研结论与用户实际需求偏差 | Day 1 完成后让用户看效果，Day 2 按反馈调整 |
| 子代理 3 个失败 | 我自己兜底（execute_code 全能做） |

---

## 状态跟踪

每完成一项 → git commit + 在本文件勾选。